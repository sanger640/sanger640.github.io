<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://sanger640.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://sanger640.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-09-29T22:16:08+00:00</updated><id>https://sanger640.github.io/feed.xml</id><title type="html">blank</title><entry><title type="html">Policy Optimization and Actor Critic Methods</title><link href="https://sanger640.github.io/blog/2025/actorcritic/" rel="alternate" type="text/html" title="Policy Optimization and Actor Critic Methods"/><published>2025-09-29T00:00:00+00:00</published><updated>2025-09-29T00:00:00+00:00</updated><id>https://sanger640.github.io/blog/2025/actorcritic</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/actorcritic/"><![CDATA[<h2 id="policy-optimization">Policy Optimization</h2> <ul> <li>Learn policy directly from enviornment</li> <li>Stochastic policy class smooths out the optimization problem</li> <li>Optimize the following: \(max_\theta E[\sum^H_{t=0}R(s_t) \mid \pi_\theta]\)</li> <li>Policy optimization can be simpler then $Q$ or $V$ <ul> <li>V doesn’t have transition</li> <li>argmax over actions for Q often difficult for continous actions</li> </ul> </li> </ul> <h3 id="likelihood-ratio-policy-gradient">Likelihood Ratio Policy Gradient</h3> <ul> <li>Goal is to find max expected reward over a policy: \(max_\theta U(\theta) = max_\theta \sum_\tau P(\tau; \theta)R(\tau)\)</li> <li>Where: <ul> <li>Total reward: $R(\tau) = \sum_{t=0}^HR(s_t, u_t)$</li> <li>$\tau$ is a state action sequence upto timestep $H$</li> </ul> </li> <li>Goal is to favor trajectories with higher reward</li> <li>Gradient based optimization: <ul> <li>Sample based approximation</li> <li>$\frac{\nabla_\theta P(\tau, \theta)}{P(\tau; \theta)} = \nabla_\theta log P(\tau; \theta)$</li> <li>$\nabla U(\theta) = \hat{g} = \frac{1}{m}\sum_{i=1}^m\nabla_\theta log P(\tau^i; \theta)R(\tau^i)$</li> <li>Can be used no matter what the reward function is (discontinuous/unknown)</li> <li>Only derivative with respect to the trajectory distribution</li> <li>Probability distribution over trajectories is inducing trajectories, so we can still take gradients! <strong>IMPORTANT POINT</strong></li> </ul> </li> <li>Intuition: <ul> <li>Gradient tries to inrease probability of paths with positive R</li> <li>Decrease probablity of paths with negative R</li> </ul> </li> </ul> <h3 id="temporal-decomposition">Temporal Decomposition</h3> <ul> <li>No dynamics model required for gradient update \(\nabla_\theta log P (\tau^i; \theta) = \sum_{t=0}^H\nabla_\theta log \pi_\theta(u_t^i \mid s_t^i)\)</li> <li>Get gradients from backpropagration, do rollouts and estimate $\hat{g}$</li> <li>But estimates unbiased but very noisy</li> </ul> <h2 id="actor-critic-methods">Actor-Critic Methods</h2> <ul> <li>Modification to Policy Optimizations that make it actor-critic</li> </ul> <h3 id="baseline-subtraction">Baseline subtraction</h3> <ul> <li>Subtract baseline $b$ from total reward</li> <li>Rewards from the past are not relevant, so those can be remove from the estimate</li> <li>Removing terms that don’t depend on current action can lover variance:</li> <li>New estimate: \(\frac{1}{m} \sum_{i=1}^{m} \sum_{t=0}^{H-1} \nabla_\theta \log \pi_\theta(u_t^{(i)}|s_t^{(i)}) \left( \sum_{k=t}^{H-1} R(s_k^{(i)}, u_k^{(i)}) - b(s_t^{(i)}) \right)\)</li> <li>Good choice for $b$: <ul> <li>constant baseline which is average of trajectory rollouts</li> <li>Minimum variance baseline: take weighted average, resulted in lower variance, not seen much in implementation</li> <li>Time-dependent baseline</li> <li>State-dependent expected return: essentialy learning the value function of states. <ul> <li>Per state you have a different $b$ in the gradient estimate.</li> </ul> </li> </ul> </li> <li>Increase logprob of action proportionally to how much its returns are better than the baseline (expected return) under the current policy. <ul> <li>The state dependent baseline (value function) is most intutive to understand this. (This is the critic and the policy network is the actor)</li> </ul> </li> <li>Need to learn this value function</li> <li>$R_t-b(s_t)$ is also usually called the advantage estimate or $\hat{A_t}$.</li> </ul> <h3 id="value-function-estimation">Value Function Estimation</h3> <ul> <li>Monte Carlo Estimation of $V^\pi_\phi$, regress against empirical return: \(\phi_{i+1} \leftarrow \arg\min_\phi \frac{1}{m} \sum_{i=1}^{m} \sum_{t=0}^{H-1} \left( V_\phi^\pi(s_t^{(i)}) - \left( \sum_{k=t}^{H-1} R(s_k^{(i)}, u_k^{(i)}) \right) \right)^2\)</li> <li>Can also do bootstrapping instead: <ul> <li>Do a fitted value iteration \(\phi_{i+1} \leftarrow \min_\phi \sum_{(s,u,s',r)} \|r + V_\phi^\pi(s') - V_\phi(s)\|_2^2 + \lambda\|\phi - \phi_i\|_2^2\)</li> </ul> </li> </ul> <h3 id="advantage-estimation">Advantage Estimation</h3> <ul> <li>Variance reduction by function Approximation (and discount factor) <ul> <li>approximate average reward by some steps of reward plus value function from that timestep onwards</li> </ul> </li> <li>Trading off for low variance and introducing high-bias, can to n-step estimate (similar to TD (lambda)).</li> <li>Gradient estimate for $V$:</li> </ul> \[\phi_{i+1} \leftarrow \min_\phi \sum_{(s,u,s',r)} \|\hat{Q}_i(s,u) - V_\phi^\pi(s)\|_2^2 + \kappa\|\phi - \phi_i\|_2^2\] <ul> <li>Gradient estimate for $\pi$:</li> </ul> \[\theta_{i+1} \leftarrow \theta_i + \alpha \frac{1}{m} \sum_{k=1}^{m} \sum_{t=0}^{H-1} \nabla_\theta \log \pi_\theta(u_t^{(k)}|s_t^{(k)}) \left( \hat{Q}_i(s_t^{(k)}, u_t^{(k)}) - V_{\phi_i}^\pi(s_t^{(k)}) \right)\] <h3 id="general-structure-of-algorithm">General structure of algorithm:</h3> <ol> <li>Init $\pi_{\theta_0}, V^\pi_{\phi_0}$</li> <li>Collect Rollouts ${s, u, s’, r}$ and $\hat{Q}_i(s,u)$</li> <li>Update : <ul> <li> \[\phi_{i+1} \leftarrow \min_\phi \sum_{(s,u,s',r)} \|\hat{Q}_i(s,u) - V_\phi^\pi(s)\|_2^2 + \kappa\|\phi - \phi_i\|_2^2\] </li> <li> \[\theta_{i+1} \leftarrow \theta_i + \alpha \frac{1}{m} \sum_{k=1}^{m} \sum_{t=0}^{H-1} \nabla_\theta \log \pi_\theta(u_t^{(k)}|s_t^{(k)}) \left( \hat{Q}_i(s_t^{(k)}, u_t^{(k)}) - V_{\phi_i}^\pi(s_t^{(k)}) \right)\] </li> </ul> </li> </ol> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/actor_critic-480.webp 480w,/assets/img/actor_critic-800.webp 800w,/assets/img/actor_critic-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/actor_critic.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 1: Actor-Critic Algorithm. </div> </div>]]></content><author><name></name></author><category term="explanation"/><category term="representation"/><category term="learning"/><summary type="html"><![CDATA[Policy Optimization Learn policy directly from enviornment Stochastic policy class smooths out the optimization problem Optimize the following: \(max_\theta E[\sum^H_{t=0}R(s_t) \mid \pi_\theta]\) Policy optimization can be simpler then $Q$ or $V$ V doesn’t have transition argmax over actions for Q often difficult for continous actions]]></summary></entry><entry><title type="html">World Models</title><link href="https://sanger640.github.io/blog/2025/dreamer/" rel="alternate" type="text/html" title="World Models"/><published>2025-09-28T00:00:00+00:00</published><updated>2025-09-28T00:00:00+00:00</updated><id>https://sanger640.github.io/blog/2025/dreamer</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/dreamer/"><![CDATA[<h1 id="introduction">Introduction</h1> <ul> <li>World Models are an explicit way to represent agent’s knowledge about it’s enviornment.</li> <li>When inputs are high-dim images, latent dynamics model predict ahead in an abstract latent space. <ul> <li>predicting compact representations instead of images has been hypothesized to reduce acculating errors, and can be parallelized due to its low compute footprint.</li> </ul> </li> <li>Leant from a database of past experience <ul> <li>THe actor and critic on top is learnt from imagined sequences of compac model states</li> </ul> </li> </ul> <h2 id="dreamer-v2-">Dreamer V2 <d-cite key="hafner2022masteringataridiscreteworld"></d-cite></h2> <ul> <li>Learn a World Model from past experience, use the world model for RL using Actor-Critic <ul> <li>RL in the model</li> <li>Do rollout inside the model to refine and train the Actor-Critic</li> </ul> </li> <li>Uses discrete latent representations (instead of continous) and balancing terms within KL loss.</li> </ul> <h3 id="world-model-learning">World Model learning</h3> <ul> <li>Arch: image encoder, recurrent state-space model (RSSM) to learn the dynamics, predictors for image, reward and discount factor. <ul> <li>RSSM uses a sequence of deterministic recurrent states $h_t$.</li> <li>The posterior state $z_t$ has info about the current image, while prior state $\hat{z_t}$ aims to predict posterior without access to current image.</li> </ul> </li> <li>All the components are implemented as Neural Networks. <ol> <li>Reccurent model: $h_t = f_\phi(h_{t-1}, z_{t-1}, a_{t-1})$</li> <li>Representation model: $z_t \sim q_\phi(z_t \mid h_t, x_t)$</li> <li>Transition predictor: $\hat{z_t} \sim p_\phi(\hat{z_t} \mid h_t)$</li> <li>Image predictor: $\hat{x_t} \sim p_\phi(\hat{x_t} \mid h_t, z_t)$</li> <li>Reward Predictor: $\hat{r_t} \sim p_\phi(\hat{r_t} \mid h_t, z_t)$</li> <li>Discount Predictor: $\hat{\gamma_t} \sim p_\phi(\hat{\gamma_t} \mid h_t, z_t)$</li> </ol> </li> </ul> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/world_model_learning-480.webp 480w,/assets/img/world_model_learning-800.webp 800w,/assets/img/world_model_learning-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/world_model_learning.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 1: World Model Learning. </div> </div> <ul> <li>Figure 1 shows how the Dreamer to components interact with each other.</li> <li>The transition predictor guesses the next model state without using the next image, so we can learn behavior by predicting model states without having to interact with the enviornment and observe images.</li> <li>The discount predictor allows for estimating probability of an episode ending.</li> <li>Model state is a concatenation of the deterministic $h_t$ and $\hat{z_t}$.</li> <li>ELBO loss function used to jointly optimize all components of the world model: \(\mathcal{L}(\phi) = \mathbb{E}_{q_t(\tau_t | \gamma_t, x_T^\pi)} \left[ \sum_{t=1}^T -\ln p_\phi(\sigma_t | h_t, z_t) - \ln p_\phi(r_t | h_t, z_t) - \ln p_\phi(\gamma_t | h_t, z_t) + \beta \text{KL}[q_\phi(z_t | h_t, x_t^\pi) \| p_\phi(z_t | h_t)] \right]\) <ul> <li>Uses KL-Balancing: <ul> <li>Make the prior match the representation while at the same time make the representation resemble the prior</li> <li>Which should get the preferance? <ul> <li>Minimize KL loss faster with resepect to prior than the representations. This will avoid regularizing the representations to a poorly trained prior.</li> <li>Use different learning rates $\alpha$ for prior and $1-\alpha$ for the approximate posterior.</li> <li>This encorages learning an accurate prior over increasing posterior entropy.</li> </ul> </li> </ul> </li> </ul> </li> </ul> <h3 id="behavior-learning">Behavior Learning</h3> <ul> <li>DreamerV2 learns long-horizon behaviors purely within its world model using an actor and a critic.</li> <li>Both actor and critic operate on top of the learnt model states</li> <li>The world model is fixed during behavior learning, so actor and value gradient do not affect it’s representations.</li> <li>Not predicting images allows for simulating 1000s of latent trajectories in parallel on a single GPU.</li> </ul> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/actor_critic_learning-480.webp 480w,/assets/img/actor_critic_learning-800.webp 800w,/assets/img/actor_critic_learning-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/actor_critic_learning.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 2: Behavior learning of actor-critic in the model latent states. </div> </div> <ul> <li>Imagination MDP: <ul> <li>The diagram of the training within the imagination of the world model is as follows. Time horizon is finite (around 15 timesteps).</li> <li>In this figure, trajectories start from posterior states computer during model training and predict forward by sampling actions using the actor network</li> <li>The critic learns to predic the expected sum of future rewards for each state based on actors policy. <ul> <li>Use TD learning on imagined rewards</li> <li>Actor is trained to maximize the critic prediction, via policy gradients.</li> </ul> </li> <li>Stochastic actor (for exploration) and deterministic critic: \(\hat{a_t} \sim p_\psi(\hat{a_t} \mid \hat{z_t})\) \(v_\epsilon(\hat{z}_t) = E_{p_\phi, p_\psi}[\sum_{\tau \geq t}\hat{\gamma}^{\tau-t}\hat{r}_t]\)</li> <li>Latent state is markovian, so no need for conditioning on actor critic.</li> <li>Trained from same trajectories but different loss functions for both.</li> <li>Actor outputs categorical distribution over actions, critic has deterministic output.</li> <li>Both are MLP NNs.</li> </ul> </li> <li>Critic Loss Function: <ul> <li>TD learning used (n target steps used instead of 1), defined recursively.</li> <li>Squared Loss is then optimized:</li> </ul> </li> <li>Actor Loss Function: <ul> <li>Straight through gradient used (due to categorical latents and actions) along with REINFORCE gradients</li> </ul> </li> </ul>]]></content><author><name></name></author><category term="explanation"/><category term="representation"/><category term="learning"/><summary type="html"><![CDATA[Introduction]]></summary></entry><entry><title type="html">Variational Inference</title><link href="https://sanger640.github.io/blog/2025/elbo/" rel="alternate" type="text/html" title="Variational Inference"/><published>2025-09-21T00:00:00+00:00</published><updated>2025-09-21T00:00:00+00:00</updated><id>https://sanger640.github.io/blog/2025/elbo</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/elbo/"><![CDATA[<blockquote> <p>The blog post is based primarily on the excellent overview paper by Calvin Luo, for detailed treatment of proofs and implementation check out his paper <d-cite key="luo2022understandingdiffusionmodelsunified"></d-cite>.</p> </blockquote> <h1 id="intro-surprise-and-cross-entropy">Intro, Surprise, and Cross-Entropy</h1> <ul> <li>Probability is the degree of belief ( a measure of confidence rather then frequency)</li> <li>Probability Distribution is a function that maps the state to its probability</li> <li>Sampling: generating samples from the underlying distribution. How to do this for complex distributions?</li> <li>Surprise: measure (function of probability) which is high for rare events (high surprise) and low for common events. When probabilities multiply, the surprise should add. The function that behaves this way is the logarithm of \(\frac{1}{p_s}\).</li> <li>Can charecterize entire distribution by it’s average surprise. This average (expected) surprise is known as the entropy of the distrubution and has the following formula: \(H = \sum_s p_s log(\frac{1}{p_s})\) <ul> <li>Interpretation: measure of inherent uncertainty of a distribution when predicting.</li> <li>In real world, we do not have access to the true distrubtion, we use an internal approximate model (our belief about the distribution).</li> </ul> </li> </ul> <h2 id="cross-entropy">Cross Entropy</h2> <ul> <li>What if the deviation between the internal model and the actual model is significant? <ul> <li>Cross Entropy: function of two probability distribution, which quantifies the average surprise you will get by observing a RV from dist. P, while believing in its model Q.</li> <li>The formula is as follows: \(H(P,Q) = \sum_{s}^{states} p_s log(\frac{1}{q_s})\)</li> <li>High Surprise can come from two sources: believing the wrong model or the inherent uncertainty of the process. Believing in the wrong model, can only increase surprise not decrease it.</li> <li>\(H(P,Q) != H(Q,P)\) &lt;- Assymertical -&gt;</li> </ul> </li> </ul> <h2 id="kl-divergence">KL Divergence</h2> <ul> <li>KL Divergence: $H(P,Q) - H(P) = \sum_s p_s log(\frac{p_s}{q_s})$ <ul> <li>It measures the surprise casused by the model and true distribution being different (extra surprise), beyond the uncertainty of the true distribution itself.</li> <li>Why measure this? <ul> <li>Good for learning distributions of images (in ML)</li> <li>For autoencoders <ul> <li>Loss function: KL-Divergence make as low as possible to make the output distribution as close to the input.</li> <li>Usually, framed as minimizng cross-entropy (means the same thing as H(P) is constant). Therefore, KL-Divergence can never go to zero.</li> </ul> </li> </ul> </li> </ul> </li> </ul> <h1 id="variational-inference">Variational Inference</h1> <ul> <li>How to build and utilize world models from partial information? <ul> <li>Use variational inference</li> </ul> </li> <li>Describing complex underlying distrubution effectively with limited sampling. <ul> <li>Observation (x) will be determined by a hidden latent variable (z) with its own distribution P(z).</li> <li>Latent variables can be though using Plato’s Allegory of the cave. Some underlying variable which influences the observations.</li> <li>No need to find it, let the model learn it.</li> <li>For a given z, find the $P(x\mid z)$.</li> <li>Joint probability is: $P(x, z) = P(z) * P(x\mid z)$.</li> </ul> </li> <li>How to represent distribution? <ul> <li>Use points? Too cumbersome</li> <li>Use parameteric approach: represent dist. with few key parameters. Ex: Gaussian Distribution (only 2 parameters)</li> <li>For multi-dimensional data use the higher dimension joint gaussian version (Covariance matrix and mean vector)</li> <li>NN can output these 12 numbers but will cause problem in higher dimensions.</li> <li>Common assumption: covariance matrix is fixed, isotropic gaussian, only mean needed to be outputted by the NN.</li> <li>NN is outputting the parameters of the prob.distribution. Hence, for a sample $z$, we out a probability $P(x\mid z)$.</li> </ul> </li> <li>Objective is to minimize the error (some distance) between distribution $P(x), P_\theta (x)$ <ul> <li>Minimize KL Divergence -&gt; maximize $\sum_x^{states} P(x) log P_\theta (x)$</li> <li>But true data distribution is unknown, but we have access to finite sample (our dataset)!</li> <li>Allows us to approximate the expectation over the true underlying distribution using a simple average.</li> <li>Now, estimate $P_\theta (x)$ -&gt; we can get $P_\theta (x\mid z)$ using latent to outputting distribution parameters.</li> <li>Many different z values could have generated a specific x value, so need to consider all possible latent values: $P_\theta (x) = \sum_z P_\theta (x\mid z)P_\theta(z)$ (called likelihood)</li> <li>Hence, model allows needs to learn the parameters of the prior (distribution of z)</li> </ul> </li> </ul> <h2 id="training-loop-in-a-nutshell-to-learn-model-distribution-from-samples-given-we-know-pz">Training loop in a nutshell to learn model distribution from samples (given we know P(z))</h2> <ol> <li>Take a data point (x) from training dataset</li> <li>Randomly sample many candidate $z_k$ from the distribution $P(z)$ (prior already known, what if we don’t know?)</li> <li>Map each $z_k$ through the NN and get parameters for the $P_\theta(x\mid z_k)$ for each $z_k$.</li> <li>For each $z_k$ compute the $P_\theta(x\mid z_k)$</li> <li>Compute average log-likelihood $log[\sum_z^{z_k} P_\theta (x\mid z)P_\theta(z)]$</li> <li>Iteratively update (using gradient descent) $\theta$ to maximize the log-likelihood across the dataset.</li> </ol> <h2 id="higher-dimenstional-latent-space-and-evidence-lower-bound">Higher-Dimenstional latent space and Evidence lower bound</h2> <ul> <li>Curse of dimensionality: sampling grows exponentially with number of dimensions for adequate coverage.</li> <li>But most latents don’t explain the data</li> <li>Use importance sampling!</li> <li>Better to oversample rare and important cases, and mathamatically correct for the bias, rather then risk missing them entirely! <ul> <li>How to find these “important” regions in the latent space</li> <li>Instead of blindly sampling from the entire latent space, we train a seperate neural network to do this!</li> <li>This network learns to predict which region in the latent space have likely generated each data point. <ul> <li>NN leans a variational distribution: $Q_\theta (z\mid x)$</li> </ul> </li> <li>Can try optimize the parameters of $Q_\theta$ and $P_\theta$ directly by maximizing: $log P_\theta (x) = log E_{z~Q_\theta (z\mid x)} [P_\theta (x\mid z) \frac{P_\theta(x\mid z)}{Q_\theta(z\mid x)}]$ but is probelematic.</li> <li>Fundamental issue is the log of the expectation (average) is hard to optimize (noisy and unstable).</li> </ul> </li> <li>Jensen’s Inequality: <ul> <li> \[log E[X] \geq E[log X]\] </li> <li>Hence, by swapping the possition of expectation and log from our objective above, we can get a lower bound on our objective (Evidence lower bound -&gt; ELBO). If we maximize this lower bound, we automatically maximize our objective.</li> <li>Computationally, much easier to maximize ELBO.</li> <li>Has intuitive meaning for the terms: <ul> <li>Accuracy term : $E[logP_\theta(x\mid z)]$</li> <li>Complexity term: $D_{KL}[Q_\theta(z\mid x)\mid \mid P_\theta(z)]$</li> </ul> </li> </ul> </li> </ul> <h3 id="implementation-notes-about-the-prior-p_theta-z">Implementation Notes about the prior $P_\theta (z)$</h3> <ul> <li>In theory, can learn the distribution of the prior distribution.</li> <li>But typically, prior is fixed to be an isotropic gaussian, with zero mean and identity covariance.</li> <li>Only learn the weight of the recognition model $Q_\theta(z\mid x)$ and the generative model $P_\theta (x \mid z)$</li> </ul> <h2 id="updated-training-loop">Updated Training loop</h2> <ol> <li>Take a data point (x) from training dataset</li> <li>Pass through NN of recognition model to get the parameters of the importance sampling model $Q_\theta(z\mid x)$.</li> <li>Use the $Q_\theta$ distributon to effectively sample ($z_k$)from the $P_\theta(z)$ prior distribution.</li> <li>Map each $z_k$ through the NN and get parameters for the $P_\theta(x\mid z_k)$ for each $z_k$.</li> <li>For each $z_k$ compute the $P_\theta(x \mid z_k)$</li> <li>Compute the ELBO objective (which is to be maximized): \(E[logP_\theta(x\mid z)] - D_{KL}[Q_\theta(z\mid x)\mid \mid P_\theta(z)]\)</li> <li>Iteratively update (using gradient descent) $\theta$ to maximize the ELBO objective across the dataset.</li> </ol> <p>Note: both recognition and generative model do not output the distribution directly, just the parameters. So have to put these into the formula for the gaussian directly to get samples.</p> <p>The accuracy formula in ELBO is proportiaonal to the following: $log P_\theta (x \mid z) ~ - \mid \mid x-\mu \mid \mid ^2$ with the isotropic gaussian assumption!</p> <p>Also the KL-divergence term (using this gaussian assumption) in ELBO can also be calculated using only the model parameters, hence ELBO is an ideal objective for training, as literal sampling each step is not required.</p> <h1 id="variational-autoencoders-vae">Variational Autoencoders (VAE)</h1> <ul> <li>In the default VAE formulation, ELBO is directly maximized.</li> <li>Typical autoencoder: encoder and decoder <ul> <li>Latent space $Q_\psi$ is smaller then the observation space</li> <li>Observation (input) data is trained to predict itself after undergoing an intermediate bottlenecking representation step (latent).</li> <li>Each input $x$ results in a distribution over possible latents $Q_\psi(z\mid x)$</li> <li>This distribution of latents is converted into an output observation using a learnt deterministic decoder $P_\theta (x \mid z)$.</li> </ul> </li> <li>ELBO is optimized jointly over $\psi$ and $\theta$. THe encoder $Q_\psi(z \mid x)$ is usually a multivariate Gaussian with diagonal covariance and the prior $P(z)$ (our assumption of the true distribution of the latent) is often standar multivariate Gaussian. $Q_\psi (z \mid x) = \mathcal{N} (z; \mu_\psi (x), \sigma_\psi^2 (x) I)$ $P(z) = \mathcal{N}(z; 0 , I)$</li> <li>The reconstruction term in ELBO is maximized using Monte Carlo estimates while the divergence term is calculated analytically. <ul> <li>Stochastic sampling is non-differentiablem, hece reparameterization trick needed to be able to differentiate the parameters. $z = \mu_\psi (x) + \sigma_\psi (x) \epsilon$ $\epsilon ~ \mathcal{N}(\epsilon; 0, I)$</li> </ul> </li> <li>After training, generating new data is performed by sampling directly from the latent space $P(z)$ and then running it through the decoder.</li> </ul> <h2 id="hierarchical-variational-encoders-hvae">Hierarchical Variational Encoders (HVAE)</h2> <ul> <li>Generalization of a VAE that extent to multiple hierarchies (layers) over latent variables.</li> <li>Markovian HVAE: each transition in the hierarchy is Markovian. Decoding each latent $z_t$ only conditions on the previous latent $z_{t+1}$. <ul> <li>Basically stacking VAEs on top of each other. (recursive VAE)</li> </ul> </li> </ul> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/vae-480.webp 480w,/assets/img/vae-800.webp 800w,/assets/img/vae-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/vae.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 1: VAE depicted by the arrows: the forward arrow is the encoder ($q(z \mid x)$) which defines a distribution over the latent $z$ for observation $x$; and the backward arrow is the decoder ($p(x \mid z)$) which defines a distribution over the observation $x$ for some sampled latents. </div> </div> <h2 id="variational-diffusion-models-vdm">Variational Diffusion Models (VDM)</h2> <ul> <li>VDM is mVAE with 3 restrictions: <ol> <li>Latent dimension is exactly equal to the data dimension</li> <li>Structure of the latent encoder at each time step is not learned, it is predefined as a linear Gaussian model. (Gaussian dist. centered around the output of the previous timestep)</li> <li>The Gaussian parameters of the latent encoders vary over time in such a way that the distribution of the latent at timestep T is a standard Gaussian.</li> </ol> </li> <li>In VDM, we are only intreseted in learning the decoder conditionals $p_\theta(x_{t-1} \mid x_{t})$ so that we can simulate new data. The encoder distribution $q(x_t \mid x_{t-1})$ are no longer parameterized by $\psi$, instead are completely modelled as Gaussians with defined mean an variance parameters at each timestep. <ul> <li>The encoder transitions are defined as: \(q(x \mid x_{t-1}) = \mathcal{N} (x_t; \sqrt{\alpha_t}x_{t-1}, (1-\alpha_t)I)\)</li> </ul> </li> </ul> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hvae-480.webp 480w,/assets/img/hvae-800.webp 800w,/assets/img/hvae-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hvae.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 2: VDM as a hierarchy of forward and reverse diffusion processes. The aim of the diffusion model is to minimize the difference between the distribution between the pink (Gaussian noise corruption step) and the green (denoising step) arrows. </div> </div> <ul> <li> <p>After optimizng, the sampling procedure is sampling Gaussian noise from $p(x_T)$ and iteratively running the denoising transitions $p_\theta(x_{t-1} \mid x_{t})$ for T steps to generate a novel $x_0$.</p> </li> <li> <p>Using reparameterization trick recursively, along with markov and gaussian assumptions, the optimization objective simplifies to the following:</p> </li> </ul> \[\arg\min_{\theta} \; \mathbb{E}_{t \sim \mathcal{U}(2, T)} \left[ \mathbb{E}_{q(x_t | x_0)} \left[ D_{\mathrm{KL}} \big( q(x_{t-1} | x_t, x_0) \; \|\; p_\theta(x_{t-1} | x_t) \big) \right] \right]\] <ul> <li> <p>The above stochastic optimization objective can be interpreted as finding the parameters (of a neural network) that minimizes the expected KL-Divergence between the true reverse diffusion process and our learned reverse process.</p> </li> <li> <p>The outer expectation during training can be replaced by randomly sampling different data points from the (training) dataset.</p> </li> <li> <p>The inner expectation can be replaced by uniformly sampling different noise levels (timesteps t).</p> <h3 id="three-interpretations-of-the-d_kl-term">Three interpretations of the $D_{KL}$ term:</h3> </li> </ul> <h4 id="reconstructing-the-ground-truth-image">Reconstructing the ground truth image</h4> <ul> <li> <p>One way to break down the $D_{KL}$ term is as follows:</p> </li> <li> <p>Here, optimizing the VDM boils down to learning a neural network to predict the original ground truth image from an arbitrarily noisified version of it.</p> </li> </ul> \[D_{\mathrm{KL}} \big( q(x_{t-1} | x_t, x_0) \; \|\; p_\theta(x_{t-1} | x_t) \big) = \frac{1}{2 \sigma_q^2(t)} \frac{\bar{\alpha}_{t-1} (1-\alpha_t)^2}{(1-\bar{\alpha}_t)^2} \left\| \hat{\mathbf{x}}_\theta(\mathbf{x}_t, t) - \mathbf{x}_0 \right\|_2^2\] <h4 id="predicting-source-noise">Predicting source noise</h4> <ul> <li> <p>Here, we learn $\epsilon_\theta^hat (x_t, t)$ which is a neural network that is trying to predict the source noise $\epsilon_0 ~ \mathcal{N} (\epsilon; 0, I)$ that determines $x_t$ from $x_0$.</p> </li> <li> <p>The objective $D_{KL}$ is now framed as:</p> </li> </ul> \[D_{\mathrm{KL}} \big( q(x_{t-1} | x_t, x_0) \; \|\; p_\theta(x_{t-1} | x_t) \big) = \frac{1}{2 \sigma_q^2(t)} \, \frac{(1 - \alpha_t)^2}{(1 - \bar{\alpha}_t) \alpha_t} \left\| \epsilon_0 - \hat{\epsilon}_\theta(x_t, t) \right\|_2^2\] <h4 id="score-based-optimization">Score-based optimization</h4> <ul> <li> <p>The third way is to estimate $s_\theta (x_t, t)$ which is a neural network that learns to predict the score function $\nabla_{x_t} log p(x_t)$, which is the gradient of x_t in data space, for any arbitrary noise level t.</p> </li> <li> <p>The objective $D_{KL}$ is now framed as:</p> </li> </ul> \[D_{\mathrm{KL}} \big( q(x_{t-1} | x_t, x_0) \; \|\; p_\theta(x_{t-1} | x_t) \big) = \frac{1}{2 \sigma_q^2(t)} \, \frac{(1 - \alpha_t)^2}{(1 - \bar{\alpha}_t) \alpha_t} \left\| s_\theta(x_t,t) - \nabla log p(x_t) \right\|_2^2\] <p>NOTE: Here, \(p(x) = q(x_t \mid x_0) = \mathcal{N}(x_t; \sqrt(\bar{\alpha}_t)x_0, (1- \sqrt(\bar{\alpha}_t)I))\) is called Denoising Score Matching (DSM)</p> <ul> <li>To generalize score-based generative diffusion models to infiite timesteps, represent the evolution of the perturbation of the image as a stochastic differential equation (SDE). <ul> <li>Sampling is then performed by reversing the SDE</li> </ul> </li> </ul> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/diffmodel-480.webp 480w,/assets/img/diffmodel-800.webp 800w,/assets/img/diffmodel-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/diffmodel.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 3: Score based interpretation of the diffusion optimization objective. Optimizing for the score is like moving to one of the modes in the distribution at each time step. The log gradient step defines a vector field along which a randomly sampled data with evolve in time until it converges to one of the modes. </div> </div> <h2 id="conditional-guidance-for-diffusion-models">Conditional Guidance for Diffusion Models</h2> <ul> <li> <p>To explicitly control the data we generate through some information $y$ (ex: text, or low res image), we also need to learn the conditional distribution. This forms the backbone of image-text models and super-resolution models.</p> </li> <li> <p>Natural way to add conditioning is simply alongside the timestep:</p> </li> </ul> \[p(x_{0:T}) = p(x_T) \prod_{t=1}^{T} p_\theta(x_{t-1} \mid x_t, y)\] <ul> <li>“Guidance” is needed to more explicitly control the amount of importance the model gives to the conditioning information at the cost of sample diversity.</li> </ul> <h3 id="classifier-guidance">Classifier Guidance</h3> <ul> <li>The conditional ground truth score can be expanded (using Bayes Rule) to:</li> </ul> \[\nabla log p (x_t \mid y) = \nabla log p(x_t) + \nabla log p(y \mid x_t)\] <ul> <li>The adversarial gradient term $\nabla log p(y \mid x_t)$ is represented as a classifier that takes in arbitrary noisy $x_t$ and attempts to predict conditional information $y$. The classifier is seperately learnt.</li> <li>The score of the unconditional diffusion model $\nabla log p(x_t)$ is learned as previously described.</li> <li>During sampling procedure, the overall score function is computed as the sum of both.</li> <li>For finer control (either to encorage or discourage the influence of the conditional), a hyperparameter term is introduced:</li> </ul> \[\nabla log p (x_t \mid y) = \nabla log p(x_t) + \gamma \nabla log p(y \mid x_t)\] <h3 id="classifier-free-guidance-much-more-robust-in-sample-diversity-and-flexible">Classifier Free Guidance (much more robust in sample diversity, and flexible)</h3> <ul> <li>No need to train a seperate classifer.</li> <li>Learn both conditional and unconditional diffusion models together as a singular conditional model.</li> <li>The conditional ground truth score then simplifies to: \(\nabla log p (x_t \mid y) = \gamma \nabla log p(x_t \mid y) + (1- \gamma) \nabla log p(x_t)\)</li> <li>During training, model is trained on conditional data and unconditional (the conditional information query is replaced with empty string). <ul> <li>This is usually done by randomly dropping the condition during training (conditioning dropout).</li> <li>The model learns to denoise both with and without conditioning.</li> </ul> </li> <li>During the sampling (generation) phase: <ul> <li>Run model with no conditioning and with desired conditioning</li> <li>Combine the two predictions using the above weighted average.</li> </ul> </li> </ul>]]></content><author><name></name></author><category term="explanation"/><category term="statistics"/><summary type="html"><![CDATA[Introduction to how Generative models work]]></summary></entry><entry><title type="html">Safety Filters in Robotics</title><link href="https://sanger640.github.io/blog/2025/safe/" rel="alternate" type="text/html" title="Safety Filters in Robotics"/><published>2025-06-15T00:00:00+00:00</published><updated>2025-06-15T00:00:00+00:00</updated><id>https://sanger640.github.io/blog/2025/safe</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/safe/"><![CDATA[<h2 id="introduction-and-terminology">Introduction and Terminology</h2> <p>Designing robots with safety in mind is crucial to prevent the robot from crashing into obstacles and harming itself or worse, harming a human or their possessions. In most applications, safety is very much a requirement and not a feature which can be adjusted for other metrics. Hence, we need modules on the robots which gurantee safety and are mathamaically verifiable.</p> <p>Before discussing methodologies, it is useful to mention some methodologies and provide more nuanced context to the problem of safety.</p> <p>Mathamatically, the unsafe conditions of a robot can be represented by a set $F \subseteq X$ where the robot states is not allowed to enter. This set is usally called the constraint set which needs to be enforced during planning and control stages of the robot.</p> <p>But complement of the constraint is not necessarily safe, for example, a drone not currently in collision is still unsafe if it is flying toward an obstacle to faast to avoid it. A safe is rather a set from which the robot can ensure safety at all future times. This will motivate the methods inspired from positive invariance later on. A positive invariant set is a set from which no trajectory can escape in forward time, in simpler terms, if a trajectory starts inside the set it cannot leave it.</p> <p>Stability is often discussed with safety as it also relies on the concept of positive invariance, but they are two different concepts and often their is a tradeoff between the two. Refer to my Lyapunov analysis piece to learn more about it!</p> <p>When we think of safety, we also have to account for uncertainties of the real world, the sensor noise and the dynamics model of the robot. These uncertainties give rise to either robust safety gurantees (if we know the bound of the uncertainties) or probabilitic safety gurantees (high uncertainty and bound unknown). Ideally, we would want robust gurantees but in certain cases it cannot be achieved so we have to settle for probabilistic gurantees.</p> <p>(<strong>Important note</strong>: This article is primarily based on the paper by <d-cite key="safeFilter"></d-cite>)</p> <h3 id="what-is-safety-how-to-represent-it">What is safety? How to represent it?</h3> <p>As an aside, I think it is a useful exercise to contemplate on the definition of safety in the first place. Traditionally, in robotics, safety is usually framed as a collision avoidance problem, but I think that is a very naive way to frame it. There are robot tasks which require active contact with enviornment (ex: quadrapeds, inspection robots, etc.) and there are tasks where safety has a much more abstract definition (ex: prevent spillage during cooking). Hence, we need a more generalizable way to represent safety specifications for a given taks.</p> <p>Still even under this context, the framework of safe and unsafe sets still applies, just the representation of these sets have to be represented more abstractly in it’s “latent space”. This can be achieved, for example, by having a learnt safety classifer network on top of a learnt dynamics model (also called a world model) and running any of the methods mentioned in this article *. This will allow the robot to learn the failure set and plan around it accordingly. Of course, as learning is introduced this will lead to more probablistic gurantee on learning, but still is a step forward in accounting for more complex safety specifications.</p> <p>The rest of the article tackles the safety problem from the low-dimensional typical “collision avoidance” problem, but it is good to keep in mind that “collision avoidance” can be applied in a more abstract way.</p> <h2 id="safety-vs-performance">Safety vs Performance</h2> <p>As seen previously with stability, performance metrics and safety cannot be both optimally achieved in most cases, there has to a compromise. Usually, our lives are more important, so we make safety a constraint and with that constraint we try to maximize the performance metrics. This formulation can be written as follows:</p> \[J^*(x_0) = min_\pi J(x_0, \pi)\] \[subject to = safety condition\] <h2 id="safety-filters">Safety Filters</h2> <p>The above formulation of safety-performance problem can be achieved either by a single controller or two seperate controllers: one focusing on performance and the other correcting the first if the action enters the unsafe zone. Using just a single controller to account for both performance and safety is possible (like MPC) but usually is limited in it’s application and scalability. Seperating the performance and safety problem is hypothesized to achieve faster computation time and prevent overly restrive control actions.</p> <p>Having this seperation principle also enable the concept of “Safe Learning” where development of safety-agnostic learnt task policies can be complemented with task-agnostic safety filters. This will allow the development of safe RL approches which can freely explore and update it’s policies without damanging the robot or its enviornment.</p> <h3 id="anatomy">Anatomy</h3> <p>The safety filter can be divided into two modules: monitoring and intervention. The monitoring module continously supervises the robot’s course of action and determines whether it is safe or not. If unsafe, the filter will intervene by modulating/overriding the proposed control input to enforce the safety constraint. The intervention is usually based on a fallback strategy which might be manually designed, computed offline or during runtime using an optimization based process. Figure 1 shows a brief overview of this anatomy *.</p> <hr/> <h2 id="hamilton-jacobi-reachability-analysis-using-dynammic-programming">Hamilton-Jacobi Reachability analysis (using Dynammic Programming)</h2> <p>Hamilton-Jacobi (HJ) Reachability Analysis is a framework for identifying when present actions will cause future failtures and for computing best policies that minimizes failures. It assumes access to the state space, bounded non-linear dynamics function, and a compact action/control set. A domain expert is also needed to first specify what safety is in a given context by specifying a failure set on the state space.</p> <p>Given this scenario, HJ reachability will compute two things:</p> <ol> <li>Safety Monitor (V) which quantifies if the robot is in an unsafe state (doomed to collide)</li> <li>Safety Preserving Policy ($\pi$)</li> </ol> <p>More formally, it allows the computation of a least restrictive or maximal safe set using backward induction principles from dynamic programing (DP). HJ reachability analysis can be though of as a pursuit-evasion game between the controller trying to avoid failure set while an adverserial agent aims to steer the system into the failure set. It is formulated for discrete time as follows:</p> \[\] <p>In essence, it is analgous to Value Iteration, where you try to find the value of the states, given the immediate rewards are accessible. Similarly, using DP the above reachability function will converge for each state, given the initial distance between the obstacles and the current state is defined. Look at Figure * for a better illustration.</p> <p>In practice, results in control chatter and erratic motion near safe set boundary due to constant switching between the fall-back policy and the task policy. Furthermore, due to DP requiring discretization this method is quite resourse intensive and unrealastic for systems with more than six continuous state variables.</p> <p>Instead of trying to find the maximal safe set, focusing on a more conservative estimate can relax the computational requirement and that is where control barrier functions come in.</p> <h2 id="control-barrier-functions-valueenergy-based-safety-filters">Control Barrier Functions (Value/Energy Based Safety Filters)</h2> <p>Control Barrier Functions (CBFs) are one way of ensuring safety by defining the safe set with forward invariance. This condition is imposed as follows (refer to the Lyapunov article for a detailed discussion):</p> \[\] <p>These can be integrated with Convex Quadratic Programs (jointly called CBF-QP) to create a safety filter. This filter has been used in many applications with various flavors that deal with discrete time formulation, perceptual uncertainty, etc. Even with such attractive properties, CBFs are challenging to implement as finding a valid CBF for a system is often difficult and non-trivial. Although recent advancements in data driven methods to find CBFs have shown promise, they still suffer from scalability issues, especially in high-dimensional systems.</p> <h2 id="model-predictive-control-rollout-based-safety-filters">Model Predictive Control (Rollout Based Safety Filters)</h2> <p>To solve the scalability issues HJ and CBFs, rollout based filters are generally impoyed. These methods aim to verify safety at runtime by simulating the dynamics (“rollouts”) into the future or solving a trajectory optimizaiton problem.</p> <ul> <li>Tube based MPC inspired from contraction theory</li> </ul> <h2 id="data-driven-filters">Data Driven Filters</h2> <ul> <li>Only probablistic gurantees</li> <li>Scalable</li> <li>Better Runtime</li> <li>learning CBF from demponstration</li> <li>no gurantees</li> </ul>]]></content><author><name></name></author><category term="explanation"/><category term="robotics"/><summary type="html"><![CDATA[Summary of existing approaches to gurantee safety in robotic systems.]]></summary></entry><entry><title type="html">Lyapunov Functions, foundation of nonlinear system analyis.</title><link href="https://sanger640.github.io/blog/2025/lyap/" rel="alternate" type="text/html" title="Lyapunov Functions, foundation of nonlinear system analyis."/><published>2025-04-17T00:00:00+00:00</published><updated>2025-04-17T00:00:00+00:00</updated><id>https://sanger640.github.io/blog/2025/lyap</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/lyap/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Non-linear systems are notoriously hard to analyze and finding a solution trajectory for such systems is usually analytically intractable and numerically expensive. Even the numerical solutions have artificats in them, and do not give the global picture (ex: global stability) of the system. Therefore, a formal way of analyzing the gloabl behavior of such systems was needed and that was fulfilled through the concept of Lyapunov functions.</p> <h2 id="what-are-lyapunov-functions">What are Lyapunov Functions?</h2> <p>Lyapunov functions referered to as $V(x)$ are positive definite for all states ($x$) of the system, execpt when $x = 0$, and the time (or the lie derivative) of $V$ ($\dot{V}(x)$) is less than or equal to $0$. To write the conditions more formally:</p> \[V(x) \succ 0, \forall x \ne 0 \tag{1}\] \[-\dot{V}(x) \succeq 0, \forall x \tag{2}\] <p>If you can find such a function for a system, you can conclusively make claims about the stability of the origin. For example, if $\dot{V}(x)$ is strictly negative and the domain of the lyapunov is the domain of the system, then the system is globally asymptotically stable (all trajectories eventually converge to the origin). If $\dot{V}(x)$ is semidefinite in the domain, then the origin is stable (or Lyapunov stable), meaning if a point starts of “near” the origin, it will stay “near” the origin as the system evolves with time. Again, if the domain is the entirety of the system domain, then the origin is globally stable.</p> <p>If the domain is a subset of the system’s domain, the we can only say that the origin is locally stable/asymtotically stable for $V(x) \leq \rho$.</p> <p>As a side, if a function does not satisfy $(1)$ and $(2)$, then it does not mean the system is unstable. We can draw no conclusions from such a function.</p> <p>One way to think about Lyapunov functions is to compare it to the concept of Energy. Actually, one suitable Lyapunov candidate functions for mechanical system is energy as seen through the example in Figure 1.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lyapEnergy-480.webp 480w,/assets/img/lyapEnergy-800.webp 800w,/assets/img/lyapEnergy-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lyapEnergy.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 1: Total Energy Lyapunov function for inverted pendulum system where the color represents the time derivative of Lyapunov. As energy meets requirements (1) and (2), the origin is globally asymptotically stable. </div> </div> <h2 id="computing-lyapunov-functions">Computing Lyapunov Functions</h2> <p>Typically, for most systems (even those physical in nature) it is hard to find a lyapunov function which can give conclusive evidence regarding the stability of the system. Other candidates usually used are quadratic functions of the form: $V(x) = x^TPx$ where $P \succ 0$. This form usually requires trial and error to find the correct coeffecients of $P$ for non-linear systems, while for linear systems you can use Semi-Definite Programming (SDP) to find $P$ from <a href="https://stanford.edu/class/ee363/notes/lq-lyap-notes.pdf">Lyapunov’s Equation</a>. This methodology can be extended to non-linear system thorough linearization, but provides extremely conservative estimates for the region of attraction (local regions where the trajectory of a system converges to a point $x_o$).</p> <p>If the non-linear system is polynomial (or can be closedly approximated as a polynomial), sums-of-squares programming can be used to find $V(x)$ of usually quadratic or quartic power. This method was applied to the control of the inverted pendulum in the LQR-Trees paper <d-cite key="tedrake2010lqr"></d-cite>. In this paper, the cost-to-go $J(x)$ of a linear quadratic regualtor (LQR) was used as the Lyapaunov candidate function for a closed loop system at a goal point and SOS-programming was used to find the largest sub-level set ($V(x) \leq \rho$ where $\dot{V}(x) &lt; 0$) as an estimate of the region of attraction. Figure 2 shows the Region of Attraction estimate of the inverted pendulum problem at $(0, \pi)$ using SOS programming.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/roa-480.webp 480w,/assets/img/roa-800.webp 800w,/assets/img/roa-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/roa.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 2: Region of Attraction estimation using Lyapunov Functions for the inverted pendulum with satuator constraints. </div> </div> <p>In reality, polynomial approximation of systems can be quite restrictive and numerical issues can arise during the optimization process where Lyapunov conditions can be violated. Furthermore, SDP and SOS programming scale poorly with number of states in the system, limiting them to low-dimensional systems. Even some provem globally aspymoptotically stable systems, have no polynomial Lyapunov function associated with them. Hence, more generalized representations of Lyapunov functions are needed which are scalable i.e. time to use Neural Networks!</p> <h2 id="neural-networks-as-lyapunovs">Neural Networks as Lyapunovs</h2> <p>As nerual networks are universal funcion approximators, it is only natural to use them to learn Lyapunov Candidates for a system. With neural networks you are not restricted to simple quadratic/quintic representation of Lyapunov function, the possibilities of functions to consider are virtually endless. Many in academia have used neural networks to learn lyapunov functions of the system as well as the control policy simultaneously. More detailed information of the parameterization of the networks can be found in the survey paper by Dawson <d-cite key="10015199"></d-cite>.</p> <p>Most of these formulation have the same structure, a neural network to learn a Lyapunov function and a controller (with a cost function to optimize towards the Lyapunov conditions) and a verifyer to check whether the learned function meets the Lyapunov requirements. Typically, the check is done offline after training using SDP, Mixed Integer Programming (MIP) or Satisfiability Modulo Theories (SMT) solvers. These solvers are expensive to use for verification, hence this process is not done online.</p> <p>For example, in <d-cite key="pmlr-v164-dawson22a"></d-cite>, the lyapunov is initialized randomly and the controller network is initialized witht the LQR solution. Without loss of generality, the loss function can be losely defined as follows:</p> \[L(\theta, u) = V_{\theta}(0)^2 + max(0, -V_{\theta}(x)) + max(0,\nabla_{f_u}V_{\theta}(x))\] <p>In this loss formulation, if all the lyapunov conditions are satisfied then the loss will be zero, else it will be positive. The training process utilizes training by counter-examples and it is as follows:</p> <ol> <li>A random state is sampled uniformly in the state space</li> <li>The $V(x)$ and $u(x)$ are calculated</li> <li>Loss is calculated for $V(x)$</li> <li>If loss is greater than 0 (Lyapunov conditions are violated)</li> <li>The parameters of $V(x)$ are updated via gradient descent</li> <li>Process continues until convergence (certain amount of steps for which loss is 0 consecutively)</li> </ol> <p>In this training process, you can get a probabilistic certainty that the $V(x)$ does not violate the lyapunov conditions, but since then, other paper have been published which utilize formal solvers to check network and generate counter-examples for further training of the network until convergence. If no more counter-examples can be produced by the formal solver, then the network is lyapunov and the corresponding controller guarantees stability <d-cite key="NEURIPS2019_2647c1db"></d-cite>.</p> <p>But what about the region of attraction? How do you get an estimate of that using the trained Lyapunov function. This is still and open area of research, one method starts of with a small bounded region, trains the lyapunov function until convergence, and iteratively keeps expanding the box until convergence seems infeasible. In the largest box on which convergence was possible, the largest sublevel set of the trained $V(x)$ is taken as the estimate <d-cite key="DBLP:journals/corr/abs-2109-14152"></d-cite>.</p> <p>This general methodology of finding Lyapunov functions has made it easier to find larger ROA (2 to 5x the size) on non-linear control problem benchmarks like the inverted pendulum, vehicle path following and quadrotor hovering. Still, this method has its drawbacks. Even though it scales better than SOS methods and has been applied to systems with dimension 8, going beyond that still is an open problem. Furthermore, the solver are expensive to compute online and have to be done offline, so if the enviornment (dynamics) changes, the training and verification would have to be done again offline *.</p> <h2 id="applications">Applications</h2> <p>Even with the limitations of finding and computing Lyapunov functions for nonlinear systems, they have been applied successfully to a wide variety of problems. Some of these applications are briefly described here.</p> <h3 id="lqr-trees-">LQR-Trees <d-cite key="tedrake2010lqr"></d-cite></h3> <p>LQR-Trees is a feedback motion-planning algorithm for state bounded non-linear systems. This algorithm produces a policy for each state in the system. This algorithm utilizes the idea of funnels (as seen in Figure 3) to gurantee stability/tracking when travelling between two states. As long as the state starts inside the funnel, it will end at the goal state which is the output of the funnel. These funnels can be sequentially composed to cover the entire state-space and reach the goal point from any state as long as the state space is bounded.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/funnelSeq-480.webp 480w,/assets/img/funnelSeq-800.webp 800w,/assets/img/funnelSeq-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/funnelSeq.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 3: Sequential Funnels for motion planning <d-cite key="tedrake2010lqr"></d-cite>. </div> </div> <p>The “funnel” to a goal point from a set of initial point requires the use of Lyapunov functions in it’s creation. In the paper, between two points, a trajectory generation algorithm (like direct collocation) is used to get a trajectory of states and control inputs. For each of the state-input pair, the discrete algebraic ricatti equation is solved backwards (for the LQR problem) to get a cost-to-go and optimal control action. The cost-to-go is selected as the Lyapunov function for the close-loop dynamics and a SOS problem is solved for the trajectory of cost-to-go’s to create a funnel which gurantees that any point starting in the funnel stays in it and eventually converges to the goal point (outlet of the funnel).</p> <p>Using these “funnels” and RRT, LQR-Trees probabilistically covers the state-space offline for a given state space and at run-time it takes in the intial state and outputs control actions until the system reaches the goal state. Detailed treatment of this algorithm can be found in the paper <d-cite key="tedrake2010lqr"></d-cite>.</p> <h3 id="funnel-libraries-">Funnel libraries <d-cite key="majumdar2017funnellibrariesrealtimerobust"></d-cite></h3> <p>Unlike in LQR-Trees, Lyapunov functions are used as a safety gurantee in this work <d-cite key="majumdar2017funnellibrariesrealtimerobust"></d-cite>. In this work, a library of motion primitives of the desired system is created and a funnel for each of these primititves is computed offline. Unlike LQR-Trees, the aim is to create these funnels as “tight” or “small” as possible to minimize the worst-case reachable set of the system.</p> <p>During runtime, the optimal funnels are chosen from the libraries and are sequentially composed to operate the system in a safe manner in unseen enviornments. A depiction of how the algorithm operates on a plane is depicted in Figure 4.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/funnelLib-480.webp 480w,/assets/img/funnelLib-800.webp 800w,/assets/img/funnelLib-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/funnelLib.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 4: (a) Plane Deviation from nominal trajectory (b) "Funnel" of possible trajectories <d-cite key="majumdar2017funnellibrariesrealtimerobust"></d-cite>. </div> </div> <h3 id="control-barrier-functions">Control Barrier Functions</h3> <p>Control Barrier functions (CBFs) can be thought of as the transpose of Control Lyapunov functions (CLFs). While CLFs focus on the stability of dynamical systems, CBFs focus on the safety aspect. Unlike CLFs, CBFs do not require the restrictive Lyapunov conditions, they only need invariance condition $\dot{h}(x) \geq 0$ (where $h(x) \geq 0$ defines the safe set $C$) on the boundary of a subset as shown in Figure *. Hence, CBFs can be though of as a superclass of CLFs, where all CLFs are CBFs but not all CBFs are CLFs.</p> <p>Therefore, CBFs are defined a bit differently as follows <d-cite key="ames2019control"></d-cite>:</p> \[sup_{u \in U}(L_fh(x)+L_gh(x)u) \geq -\alpha(h(x))\] <p>Where $h(x)$ replaces $V(x)$ and is not necassarily positive definite, just $h(x) \geq 0$ for the safe set. In this formulation, $h(x) = 0$ at the boundary and $\alpha$ is a strictly increasing function with $\alpha(0) = 0$ that prevents $\dot{h}(x)$ from going negative too quickly (prevents infinite slope). Quadratic Programming (QP) is generally used find the controller $u(x)$ for CBF given an initial controller, the QP is formulated as follows <d-cite key="ames2019control"></d-cite>:</p> <p>\(u(x) = argmin_{u \in R^m} \frac{1}{2} ||u-k(x)||^2\) \(s.t. L_fh(x) + L_gh(x)u \geq -\alpha(h(x))\)</p> <p>If there are no input constraint, then a closed form solution can be obtained (min-norm controller), else a numerical solution is obtained. This can be combined with CLF constraint to get a multi-objective optimization problem (known as QP-CBF-CLF), where both stability and safety are solved for simultaneously, although a relaxation term is needed on stability to gurantee safety. Usually, the control inputs are of a higher relative degree, hence exponential CBFs are introduced to find solutions for such control inputs.</p> <p>QP-CBF-CLF for safe trajectory optimization has many applications such as automatic cruise control and lane keeping, dynamic walking, etc <d-cite key="ames2019control"></d-cite>.</p> <p>###</p>]]></content><author><name></name></author><category term="explanation"/><category term="robotics"/><summary type="html"><![CDATA[History of Lyapunov Functions and applications to control theory and robotics.]]></summary></entry><entry><title type="html">Safe Robotics - Handling and Manipulating Liquids</title><link href="https://sanger640.github.io/blog/2025/liquid/" rel="alternate" type="text/html" title="Safe Robotics - Handling and Manipulating Liquids"/><published>2025-04-17T00:00:00+00:00</published><updated>2025-04-17T00:00:00+00:00</updated><id>https://sanger640.github.io/blog/2025/liquid</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/liquid/"><![CDATA[<h1 id="introduction">Introduction</h1> <p>In this article, I will present the problem case of one of the research problems I am working on and how I am planning on tackling it. Shoot me a message if you have questions!</p> <h1 id="problem-statement">Problem Statement</h1> <p>One of my research topics I am looking into is robot safety, especially during manipulation. For mobile robots, safety is more so a simple collision avoidance problem where classical methods such as MPC work well, and even methods like HJ Reachability analyis can be directly applied without too much modifications. On the other hand, manipulation is a different case! In manipulation tasks, you interact with entities (humans, objects, surfaces, etc.) whose state are not so easy to define and representing safe vs failure safe is even harder to represent.</p> <p><em>Sketch of safe vs usafe set</em></p> <p>So, this is one of my main areas of research, and to evaluate various tools and methodologies I would need simple tasks with no so simple failure cases. One such task is carrying a glass full of water and pouring into another glass.</p> <p><em>Sketch of the problem</em></p> <p>This task poses the following challeges which need to be addressed:</p> <ul> <li>How to represent spillage (very “non-rigid” constraint) as failure?</li> <li>What about other failure modes: breaking/deforming the glass, turning over the recieveing glass?</li> <li>Pouring motion/angle changes with quantity/density of liquid in the container, is visual feedback enough to gauge these values? Do we need to explicitly estimate these values?</li> <li>Can the model learn the above liquid parameters, through self-supervision, by “interacting” with the object for certain time and use this information?</li> <li>How robust is the performance to changing enviornmental conditions like lighting, table color/texture, cup color?</li> </ul> <h1 id="experimental-setup">Experimental Setup</h1> <p>For this experiment, I will be using the Franka Emika Panda Powertool (Figure *). This robot arm was chosen for a number of reasons. Firstly, having a redundant joint (7dof) allows for more dexterity which is useful when handling liquids. Moreover, they have integrated torque sensors which will allow for more comprehensive algorithm design as they can help in estimating the liquid left in the container during pouring. The quick data acquisition on the platform is quite useful for trainig Imitation Learning (IL). It also has an easy ROS2 interface which is always a bonus! *</p> <p>In the experiment, an RGB sensor (Intel RealSense) will be used; it will be attached onto the end-effector and will move along with it. This will allow the robot to capture more information from different angles about the quantity of the water in the glass compared to stritcly top-down/singular angle view. As mentioned, torque sensors will also be used with the RGB sensor.</p> <p>As for the workspace, a simple desk setup will be used. The two glasses will be randomly placed in the workspace and the robot has to pick the filled glass and empty it into the empty one. The liquid color, desk color, lighting, initial amount of liquid in the glass will change between trials to gauge the robustness of the algorithms.</p> <p>In the majority of the experiment (until we are convinced our algorithm works), we will try with M&amp;Ms inside the glass instead of liquid to minimize excessive spillage while still maintaining a somewhat similar pouring effect of water. M&amp;Ms here exhibit granular flow which can be though of as a discretized version of liquid flow.Even though there are differences in the “flow” of these two, there are enough similarities for the robot to generalize to liquids.</p> <h3 id="simulation-setup-or-lack-thereof">Simulation Setup (or lack thereof)</h3> <p>The major simulators used in robotics like MuJoCo * and IsaacSim * do not support high-fidelity fluid simulation nor do they have accurate modelling of granular flow capabilities. We would still try basic simulations on these platforms, but it’s not promising.</p> <p>Another direction we could look into is to integrate high-fidelity fluid solvers (like ANSYS Fluent *) into these simulators. Isaac through it’s WARP module offers this capability, but without further testing cannot say if it would be benificial. Furthermore, the time commitment to learn ANSYS Fluent, integrate with Isaac and setup the simulation enviorment would be big. Additionally, this method will be computationally expensive as the simulation will be solving Non-Liear Navier Stokes PDE for large number of particles each timestep.</p> <p>Hence, the simulation route is still under consideration for this case study.</p> <h1 id="methodology">Methodology</h1> <p>To achieve our task of transferring M&amp;Ms/water from one container to another, we can consider either the traditional robotic pipeline or a learning based one.</p> <h2 id="traditional-pipeline">Traditional Pipeline</h2> <p>In a traditional pipeline, you have seperate and modular components with human understandable inputs and outputs. These components usually are the following:</p> <ul> <li>Perception</li> <li>Planning</li> <li>Control</li> </ul> <p>Usually, there would be additioanl components (like sensor drivers, world modelling) but can be categorized in the aforementioned three. A typical pipeline is depicted in Figure *.</p> <p>For our task, the pipeline components would have the following structure:</p> <ul> <li><strong>Perception:</strong> The perception module takes in the stream of RGB input from the camera and outputs the pose (x,y) of both the containers and their semantic labels (filled or empty). The height infromation and radius of the containers will be known apriori to simplify the experiment. A semantic segmentation output representations can also be used to get a more precise location estimate of the container and it’s geometry. Depth information can be incorporated to get an estimate of the depth as well. To create this module, pre-trained vision models (YOLO, RCNN, Vision Transformer) can be used and fine-tuned on our data with our desired outputs. An object tracker can also be added to robustly track the containers when occlusion is present (ex: robot arm blocks the view). <ul> <li><strong>Sensor Calibration:</strong> Hand-eye calibration of the RGB camera (attached to the end-effector) would be needed to transform the pose outputs to world frame.</li> <li><strong>Spillage Detection:</strong></li> </ul> </li> <li><strong>Planning:</strong> This module is responsible for taking in the pose and label of the containers (along with other geometrical properties) to generate a robot trajectory and grasp locations (and orientation) for the filled container. This module will have a state machine which will identify which phase of the task the robot is in: <ul> <li><strong>Move to filled container</strong></li> <li><strong>Pick up filled container</strong></li> <li><strong>Move to empty container</strong></li> <li><strong>Pour the liquid</strong></li> </ul> </li> <li><strong>Control:</strong> Control is responsible for following the trajectory outputted by the planning module, this would typically done using MPC which would include state constraints and actuator constraints.</li> </ul> <h3 id="drawbacks">Drawbacks</h3> <p>The aforementioned pipeline has some severe drawbacks. Firstly, our pipeline is quite brittle to scene parameters, like liquid in container, lighting, background color/texture, etc. Furthermore, this pipeline does not easily generalize to other tasks; for each custom task the components require significant modifications and hand engineering. It is also hard to implement complex safety specifications (like spillage prevention). The way it is done in this pipeline is a very crude method and to improve it would require significant hand engineering (again lack of generalizability).</p> <p>Hence, even though having sepereate modular components is good for human iterpretability, it lacks generalizability and requires significant expert knowledge (and intervention).</p> <h2 id="end-to-end-learning">End-to-end Learning</h2> <p>Another way we can approach the problem is to combine one or more such components and learn the task trajectories (and safety specs) directly from demonstrations rather then having to specify them mathamatically, which can be computationally expensive and brittle.</p> <p>Usually for this approach, the perception and planning module are combined to create something called a visuomotor policy. A visuomotor policy takes in visual observations (RGB frames) and directly outputs coordinates (velocities) in end-effector space or joint angles; these values are then passed to the low-level controllers present on the robot arm. A policy can also be made to directly output motor torque values, but generally this is a rare practice due to safety concerns.</p> <p>The policy (which is a state to action mapping) is represented by a neural network with a pretrained perception backbone which is trained using Imitation Learning, and more specifically Behavior Cloning (BC). BC basically turns control into a supervised learning problem. Where for a history of observations, the policy outputs a control action(s) which is compared to a human demonstrator’s action (from a trajectory) and based on the reconstruction loss, the policy weights are adjusted. You can find a simple training diagram in the Figure below.</p> <p><em>Training Sketch</em></p> <p>The training usually takes place for 50 to 100 human demonstrated trajectories after which the policy is good enough to reliably to the said task.</p> <h3 id="collecting-demonstrator-trajectories-and-what-works">Collecting demonstrator trajectories (and what works)</h3> <p>To collect demonstration trajectories, a simple keyboard teleop interface can be created in ROS (or someother realtime framework) where the observations from the camera and the commands sent to the robot arm (joint angle velocity or end effector velocity) is recorded in a ROS Bag (or a csv file) which can be then used to train the network.</p> <p>There are some engineering hacks and tips to keep in mind while collecting data *</p> <ul> <li>Collect your own “expert data” and don’t trust anyone else to make it perfect</li> <li>Avoid “no action” data so your policy doesn’t just sit there.</li> <li>If it is not working; collect more data until “extrapolation” becomes “interpolation”.</li> <li>Train and test on the same day because your setup might change tommorow.</li> </ul> <p>In the end, if the policy is not working well it is because we do not have enough data. There is a misconception usually with BC that the policy can never do better then the demonstrator. This is actually false, especially with scale, it is possible to generalize (abit) and do better then the demonstrator.</p> <h3 id="network-architecture">Network Architecture</h3> <p>The policy archietecture has two parts: encoder and the decoder (see Figure *). The encoder (also called the “backbone”) takes in the high dimensional RGB data and turns it into a lower dimensional latent represenatation. This latent representation is fed into the decoder to output action (or action sequences).</p> <p>For the encoder, we are planning on using ResNet-18 backbone as it is the research standard and pretrained models available.</p> <p>For the decoder, we have two choices: Diffusion models ad Action-chunking transformers (ACT). \</p> <h4 id="input-encoders">Input Encoders</h4> <ul> <li>Pretrained Vision Backbone</li> <li>Finetune output layers, with final layer having 7 outputs (joint velocities)</li> </ul> <h4 id="diffusion-model">Diffusion Model</h4> <h4 id="hardware-considerations">Hardware Considerations</h4> <ul> <li>Need to implement custom mid-level controller? <ul> <li>to generate joint pose from ee pose of policy</li> <li>each step solve diff kin problem to compute desired joint vel to track desired ee vel. resulting joint vel is Euler integrated into joint pose, which is tracked by join level controller.</li> <li>need it mostly for INTERPOLATION</li> </ul> </li> <li>The telo-op and learned policies run at 10 Hz</li> <li>Teleop done using spacemouse? <ul> <li>How?</li> </ul> </li> <li>Camera attached on gripper <ul> <li>Realsense vs GoPro</li> <li>30fps -&gt; downsampled to 320x240 at 10fps as input to learned policies</li> </ul> </li> </ul> <h3 id="implementing-safety-latent-safety-filter">Implementing safety (Latent Safety Filter)</h3> <h4 id="world-model">World Model</h4> <p>A world model allows the agent (our robot) to build representations of the world through past experiences, essentially learning the dynamics model of the task where the “states” are learnt in the latent spaces of the neural network * (Watter paper). Having a latent state space (instead of using the pixels as state) will also reduce the memory footprint.</p> <p>Core Structure of a World Model:</p> <ul> <li>Encoder (E): Learns to represent the latent spatial component, typically via a variational encoder. It is typically written as (where $z_t$ is the latent state, and $o_t$ is the observation at time t): \(z_t ~ Q_\psi (z_t| \hat{z_t}, o_t)\)</li> </ul> <p>It is analagous to a Kalman Filter.</p> <ul> <li> <p>Transition Model (TM): Learns to predict the temporal evolution of the latent states (typically via a RNN). It is represented as follows: \(\hat{z_{t+1}} ~ p_\theta(\hat{z_{t+1}} | z_t, a_t)\)</p> </li> <li> <p>Output controller Layer: takes the latent represenation from the VAE and RNN and maximize reward</p> </li> </ul> <p>** Add pic of world model **</p> <p>These world models ususally are trained using self-supervision using observation reconstruction (typical for encoders), teacher forcing, etc.</p> <p>When training this world model’s in “imagination”, an inital (random) observation from the enviornment is used and then rollouts are collected in the latent imagination for few steps (20-30) to prevent too out of distribution data.</p> <h4 id="failure-classifier">Failure Classifier</h4> <p>In HJ-Reachability, the failure set is usually the zero-sublevel set of a function $l(s)$, meaning $l(s) &lt; 0$ defines the failure set. For typical collision avoidance problem, this is defined as the signed distance function to the failure set.</p> <p>However, this approach does not work with more abstract specification of safety like spilling. Hence, we need to learn the $l(z)$ function from data by modelling it as a simple classifier over the latent states $z$.</p> <p>To train a classifer with the zero sublevel set property for the failure set, the loss needs to be the following of the network:</p> <p>$$\mathcal{L}(\mu) = \frac{1}{N_{\text{safe}}} \sum_{o^+ \in \mathcal{D}<em>{\text{safe}}} \mathrm{ReLU}\left(\delta - \ell</em>{\mu}(\mathcal{E}_{\psi}(o^+))\right)</p> <ul> <li>\frac{1}{N_{\text{fail}}} \sum_{o^- \in \mathcal{D}<em>{\text{fail}}} \mathrm{ReLU}\left(\delta + \ell</em>{\mu}(\mathcal{E}_{\psi}(o^-))\right)$$ <h4 id="hj-reachability-in-latent-space">HJ-Reachability in Latent Space</h4> </li> </ul> <p>Reachability analysis reaquires an analytic model of the system dynamics or a high-fidelity simulation to solve the fixed-point Bellman equation. In our context, we can use the world model’s dynamic model (“world models imagination”) to capture hard to design (and simulation) fluid interaction dynamics. The fixed point Bellman update is as follows:</p> \[\] <p>Note, we need a learning based approximation of this update as a tabular based version would be intractable for a 544 dimensional latent space. Also, note the reachability in this context will mean the robot will do it’s best to be safe in its representation of the world. The safety of the policy is interlinked with the accuracy of the world model in representing the enviornment.</p> <p>During deployment, the world model picks an action and imagines it’s outcome in the latent space. For the imagined outcome, it estimates the value, which will serve as the monitoring signal for whether the robot is safe or it should start applying the safety policy. The filtering law is as follows:</p> \[\] <ul> <li>DDPG is used for approximating the HJ-Value Function</li> </ul> <p>(Refer to * for more information about an overview of HJ-Reachability.)</p> <h4 id="data-collection">Data Collection</h4> <p>For world model training:</p> <ul> <li>1000 offline trajectory</li> <li>750 from sampling random action from a Gaussian Distribution</li> <li>125 trajectories which lead to spill (label the spill states)</li> <li>125 trajectories which do not spill</li> </ul> <h3 id="novel-contribution">Novel Contribution</h3> <ul> <li>Incorporate the safety filter along with policy training, optimize for both</li> <li></li> </ul> <h3 id="foundataional-models">Foundataional Models</h3> <h3 id="limitations">Limitations</h3> <ul> <li>Probabilistic Safety</li> </ul> <h3 id="misc-notes">Misc. Notes</h3> <ul> <li>Visumotor Policy (Imitation Learning)</li> <li>Behavior Cloning</li> <li>Diffusion</li> <li>Works well on long horizon task</li> <li>ALOHA <d-cite key="aloha"></d-cite></li> <li>Can you gurantee robust safety?</li> <li>Multi-task Setup Possible?</li> <li>Imitation Learning guide: https://www.youtube.com/watch?v=QX47jTL5XUM</li> </ul> <h3 id="to-do">To-Do:</h3> <ol> <li>Setup Custom Position/Velocity controller (and Inverse Kinematic)in ROS1 using <code class="language-plaintext highlighter-rouge">ros_franka</code> and <code class="language-plaintext highlighter-rouge">libfranka</code></li> <li>Setup a keyboard (and space mouse) teleop node</li> <li>Hookup Intel Realsense to the Robot and stream data</li> <li>Setup a data collection node, to synchronize, and record image stream and corresponding end effector pose/velocity.</li> <li>Data processing</li> <li>Train Diffusion Imitation Learning Policy (Baseline)</li> <li>Train Dreamer World Model and the failure classifer</li> <li>Run HJ-Reachability Analysis on the latent world model to get safety policy (HJ-RL paper)</li> <li>Compare results</li> <li>Add new contribution: <ol> <li>Optimize safety policy and imitation policy at the same time</li> <li>Replace HJ-Reachability with Control Barrier Functions <ol> <li>Generate the CBFs from trajectory (*)</li> <li>Run MPC? Is it possible? Non-linear function</li> </ol> </li> <li>Statistical Assurance (?)</li> <li>Plot vector field and analyze captured dynamics</li> <li>Lifelong learning to improve and adapt the safety filters</li> <li>Adding “robustness” to our policy (both safe and imitation) <ol> <li>Look into robust control?</li> </ol> </li> <li>…</li> </ol> </li> </ol> <h1 id="related-work">Related Work</h1> <d-cite key="semsafe"></d-cite> <d-cite key="naka"></d-cite> <d-cite key="syl1"></d-cite>]]></content><author><name></name></author><category term="explanation"/><category term="robotics"/><summary type="html"><![CDATA[Carrying and pouring open containers with liquid without spillage using robot manipulation.]]></summary></entry><entry><title type="html">Optimal Control Overview</title><link href="https://sanger640.github.io/blog/2025/optc/" rel="alternate" type="text/html" title="Optimal Control Overview"/><published>2025-04-17T00:00:00+00:00</published><updated>2025-04-17T00:00:00+00:00</updated><id>https://sanger640.github.io/blog/2025/optc</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/optc/"><![CDATA[<h1 id="introduction">Introduction</h1> <p>Control of a system (in our case a robot) is usually done using two paradigms: model-based and model agnostic.</p> <p>In this post, I will go over the model-based approach, more specifically the optimal control approach.</p> <p>A diagram showing the main components of this approach is shown in Figure 1. Each component will be analyzed in the sections below.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/opt_framework-480.webp 480w,/assets/img/opt_framework-800.webp 800w,/assets/img/opt_framework-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/opt_framework.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 1: Optimal Control Framework </div> </div> <h2 id="follow-along-example">Follow along example</h2> <p>To get a more intuitive sense, I will explain how to control an autonomous vehicle to follow a reference trajectory $(X, \beta, \psi)$ on a highway.</p> <p>This task requires controlling both the steering angle and the acceleration of the vehicle.</p> <p>For simplicity, we will decouple the problem into lateral tracking and longitudnal tracking and focus only on the lateral aspect.</p> <p>In lateral tracking, the controller aims to minimize lateral offset from a path and keep in the same heading as the reference.</p> <p>The longitudnal tracking aims to keep the vehicle at a target velocity (mostly constant). As this is ususally done via model-free controllers (PID) and for the sake of simplicity, we will neglect longitudnal control and assume longitudnal velocity for lateral tracking.</p> <p>A depiction of the lateral tracking (at constant longitudnal velocity) problem is given in Figure 2.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lat_track-480.webp 480w,/assets/img/lat_track-800.webp 800w,/assets/img/lat_track-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lat_track.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 1: Lateral Path Tracking Task </div> </div> <h3 id="reference-path">Reference Path</h3> <p>For our reference path, we will be given a set of way points (from an upstream planning module), where each waypoint is $[X_{lateral}, \beta, \dot{\psi}]$. The slip angle ($\beta$) and the heading rate (\dot{\psi}) can be found using the road curvature (which is dependent on the acceleration and velocity of our waypoints)*.</p> <h1 id="model-of-a-system">Model of a system</h1> <p>As the name suggests, a model of system to be controlled is needed for this approach to work. This model is formulated using a state space approach.</p> <p>In this approach, a system of ODE model the evolution of the system in time. Here, the state are the variables which fully describe how the relevant variables we care about change through time.</p> <p>For our example of lateral path tracking, the states are the lateral velocity from the path, and the yaw rate. The model which describes the evolution of these parameters is called the single track, linear dynamic bicycle model (as seen in Figure 3) <d-cite key="Schildbach2020"></d-cite>.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/bmodel-480.webp 480w,/assets/img/bmodel-800.webp 800w,/assets/img/bmodel-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/bmodel.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 3: Linear Bicycle Model </div> </div> <p>In this model, we assume the forces on the lateral (left and right) tires are equally distributed, hence we can collapse the car into a one track bicycle. This assumption is valid for highway driving where aggressive maneuvers are uncommon (slip angle is between 0 to 4 degrees). When aggressive steering and acceleration is needed, a higher fidelity non-linear model is required to account for the uneven force distribution in the tires.</p> <p>The state-space formulation of this model is as follows:</p> \[\dot{X} = AX + B\delta\] \[\begin{bmatrix} \dot{x} \\ \dot{\psi} \end{bmatrix} = \begin{bmatrix} -\dfrac{c_{af} + c_{ar}}{um} &amp; \dfrac{-l_f c_{af} + l_r c_{ar}}{u I_{z}} \\ \dfrac{l_f c_{af} - l_r c_{ar}}{um} + u &amp; -\dfrac{l_f^2 c_{af} + l_r^2 c_{ar}}{u I_{z}} \end{bmatrix} \begin{bmatrix} x \\ \psi \end{bmatrix} + \begin{bmatrix} \dfrac{c_{af}}{m} \\ \dfrac{a c_{af}}{I_{z}} \end{bmatrix} \delta\] <p><strong>Change to include the Khajepour paper formulation</strong></p> <p>As seen, the states evolve in time based on the current state and the steering angle, which is our control input. The objective of our controller is find the steering commands, such that it matches the trajectory. So, we will need a metric which can track this discrepancy, we need a cost function.</p> <h2 id="discrete-model-formulation">Discrete Model Formulation</h2> <p>Continuous formulation of the bicycle model cannot be directly implemented on our digital computer, hence we need to discretize our model. To discretize, we will use Forward Euler numerical integration method. Usually, RK-4 is used but for the sake of simplicity we will use Forward Euler.</p> <p>The Forward Euler formulation of our model $\dot(x) = f(x,u)$ is as follows for our preview (in our case finite horizon of 5) states $x_k$ and control inputs $u_k$ :</p> <p>\(v_1 = f(x)\) \(v_2\) \(v_3\) \(v_4\) \(x_{k+1}\)</p> <p>The predicted outputs are written using these equations for the prediction horizon, based on $x_0$ and the unknown control vector $U$ (to be found after cost optimization).</p> <h1 id="cost-function">Cost Function</h1> <p>A cost function (also called an objective or loss function in different literature) is a scalar function which provides a numerical value on the performance of our controller.</p> <p>Based on how it is formulated, the controller will act differently. Usually, the cost function has the following “quadratic” structure.</p> \[J = \int_{t = 0}^{T} \left[ \mathbf{\bar{x}}^T(t)\, \mathbf{Q}\, \mathbf{\bar{x}}(t) + \mathbf{u}^T(t)\, \mathbf{R}\, \mathbf{u}(t) \right] dt + \mathbf{x}^T(T)\, \mathbf{Q}_f\, \mathbf{x}(T), \quad \mathbf{Q} = \mathbf{Q}^T \succeq 0, \quad \mathbf{R} = \mathbf{R}^T \succ 0, \quad \mathbf{Q}_f = \mathbf{Q}_f^T \succeq 0.\] <p>In this formulation the $\bar{x}^TQ\bar{x}$ part penalizes the tracking error while the $u^TRu$ part penalizes the control effort (how aggressive you want to steer). The objective of the controller is to minimize this cost function (minimize tracking error and not be too aggressive with steering). Both the $Q$ and $R$ matrices are positive definite, and can be tuned to prioritize tracking over control effort or vice versa. Generally, this is a trial error process.</p> <p>This cost formulation can be of two types: either finite horizon or infinite horizon. In the finite horizon formulation, the cost accounts for the trajectory in the near future while in the infinite horizon case it considers the entire trajectory. For our purposes we will, stick to the finite horizon case, where $N$ represents the number of timesteps in the future the cost funtion looks forward.</p> <p>Again, a cost function can be off any form and still be valid, it can include non-linear or higher order terms. But the reason we typically use quadratic costs is because we can gurantee optimal solution using our controller, either numerically or analytically. We have solvers which can solve for control inputs which are the global minimum for our cost function. Hence, the name optimal control.</p> <p>The first solver which we will look at is Linear Quadratic Regulator (LQR) which finds an analytical for our control input.</p> <h1 id="solvers">Solvers</h1> <h2 id="linear-quadratic-regulator-lqr">Linear Quadratic Regulator (LQR)</h2> <p>LQR is one of the solvers/optimizers used to minimize the cost function. It exploits the additive nature of the cost to analytically obtain an equation for the optimal cost.</p> <p>The optimal control law turns out to be a linear gain applied to the state matrix as shown below.</p> \[u^* = -Kx\] <p>The gain is derived using the following formulation, where the S(t) matrix is found by solving a Differential Riccati equation.</p> \[K = R^{-1}B^TS\] \[-\dot{S} = SA + A^TS - SBR^{-1}B^TS + Q\] \[S(T) = Q_f\] <p><strong>Note:</strong> I will not go into the derivation of the final result, but interested readers can look into Bellman’s principle of optimality and how it is used to derive LQR control law <d-cite key="tedrake2024lqr"></d-cite>.</p> <p>This control law is guranteed to be the optimal for this cost formulation for a linear system. For our example, it will work pretty well too, and is a good starting point. But on the road, we want to gurantee safety (prevent aggressive movements) and prevent the vehicle going off the road and hitting someone. The cost function by itself can prevent deviation and minimizes aggressive maneuvers but cannot gurantee it will be within a certain safe limit. Hence, we need to account for constraints.</p> <h3 id="constraints">Constraints</h3> <p>Constraints in the context of optimal control are bounds we set to our state space and control input to keep them within a desirable range. For our example, the constraints might look something like the following.</p> <p>\(X_{left} \leq X_{pred} \leq X_{right}\) \(\delta_{left} \leq \delta_{pred} \leq_{right}\)</p> <p>Where the state constraints specify the lane boundaries we want to be within, and control constrainsts specify the steering angle limits of our vehicle we cannot exceed.</p> <p>We do not want our control values from our controller and the resulting states of our system to be outside this range under any circumstance, we need a gurantee on these bounds.</p> <p>It turns constraint makes our lives much messier and there does not exist an analytical way to optimize our cost function. We must rely on numerical optimization and that is where we turn to Model Predictive Control (MPC) for assistance.</p> <h2 id="model-predictive-control-mpc">Model Predictive Control (MPC)</h2> <p>MPC is a more general optimal control framework which accounts for constraints. It does so by leveraging numerical optimization techniques like Quadratic Programming (QP) and Convex Programming (CP).</p> <p>Both QP and CP are out of the scope of this article, but suffice to say they will find the global minimum solution if the cost is quadratic (for QP) or convex (for CP).</p> <p>Using these solvers, MPC the optimal predicted trajectory and it’s associated control inputs and selects the first one to be sent to the actual vehicle (a depiction is shown in Figure 4). This process is then repeated in the next timstep.</p> <div class="d-flex flex-column align-items-center mt-3"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/mpchorizon-480.webp 480w,/assets/img/mpchorizon-800.webp 800w,/assets/img/mpchorizon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/mpchorizon.png" class="rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption mt-2" style="max-width: 100%; text-align: center;"> Figure 4: MPC Solver Demonstration </div> </div> <p>Given numerical optmization is required in MPC, it takes more compute then LQR but with the advances in hardware, real-time operation is possible.</p> <h1 id="miscellaneous">Miscellaneous</h1> <p>This section is dedicated to things to keep in mind when implementing optimal control on a real system:</p> <ul> <li>Need to have a discrete-time formulation of the model. <ul> <li>When discretizing never use Forward-Euler (use RK4)</li> </ul> </li> <li>Make sure the parameters of the model match the parameter of the car.</li> <li>Make sure the model assumptions hold valid for the car.</li> </ul>]]></content><author><name></name></author><category term="explanation"/><category term="robotics"/><summary type="html"><![CDATA[Overview of the optimal control framework and insights into LQR and MPC]]></summary></entry><entry><title type="html">Robot Manipulation Problem</title><link href="https://sanger640.github.io/blog/2025/manip/" rel="alternate" type="text/html" title="Robot Manipulation Problem"/><published>2025-04-14T15:12:00+00:00</published><updated>2025-04-14T15:12:00+00:00</updated><id>https://sanger640.github.io/blog/2025/manip</id><content type="html" xml:base="https://sanger640.github.io/blog/2025/manip/"><![CDATA[<p>When we think of robotic manipulation we think of the standard “pick and place” task where the robot is programmed to pick up objects from one location to a goal location. In this task, the robot has to use perception to identify the object and the enviornment, planning to plan the trajectory of the rigid robot arm in the enviornment while avoiding obstacles, and control to make sure the task is being executed in realtime while accounting for changing enviornments and disturbances. The tight interplay between perception, planning and control is required to succesfully execute a robot manipulation task.</p> <p>Pick and place is just the tip of the iceberg when it comes to robot manipulation. It is the most synonymous with robot manipulation as it is the easiest to demonstrate and the most mature in terms of implementation. When we think broader, we can include any task where the robot “manipulates” or interacts with the objects around it as a robot manipulation task. This can include opening a door, solving a rubiks cube, tying shoelaces, folding laundry and the possibilities are endless.</p> <p>The performance of these manipulation tasks are evaluated against their human counterparts. As humans, we are quite adept at manipulating the world around us to acheive our objective and our manipulation capbabilities are quite efficient in “learning” and very robust to variability in enviornment and disturbances. Hence, we want our robotic clones to have comparable if not better performances in such tasks.</p> <p>But as we have found out, interacting with the enviornment is difficult for robots, partly because it is an endeavour to represent the enviornment in a formal way without making the problem intractable. With the rise of reinforcement learning and big data robotics, this problem is mostly solved with the cost (excuse the pun) of brittleness and lack of generalizability. If we can marry the learning based approaches with the traditional model-based approach which provides formal gurantees on robustness, perhaps maybe we can make robots our equals!</p>]]></content><author><name></name></author><category term="explanation"/><category term="robotics"/><summary type="html"><![CDATA[What is robotic manipulation?]]></summary></entry></feed>